package com.example.adcs.ui.main

// Template ViewModel replaced — ADCS uses DataRepository singleton directly.
// No ViewModel needed at this phase; state is managed in composables via collectAsState().
